# 萌闯星际 (Star Rush) 资源命名规范

## 1. 命名原则

### 1.1 一致性
- 所有资源命名必须保持一致的风格和格式
- 遵循项目现有的命名约定
- 同一类型资源使用相同的命名模式

### 1.2 可读性
- 命名应清晰、直观，能够准确描述资源的用途
- 避免使用缩写（除非是行业通用缩写）
- 长度适中，既不过于冗长也不过于简短

### 1.3 可维护性
- 命名应便于搜索和管理
- 考虑资源的分类和组织方式
- 支持项目的扩展和迭代

## 2. 图片资源命名规范

### 2.1 目录结构
```
StarRush/
└── images/
    ├── background/     # 背景图片
    ├── hero/           # 英雄相关图片
    ├── ui/             # UI元素图片
    ├── item/           # 物品相关图片
    └── effect/         # 特效图片
```

### 2.2 命名格式
```
[类型]_[描述]_[变体/尺寸].png/jpg
```

### 2.3 示例
- 背景图片：`background_main.png`, `background_starmap.jpg`
- 英雄图片：`hero_warrior_01.png`, `hero_mage_02.png`
- UI元素：`ui_button_blue.png`, `ui_icon_crystal.png`
- 物品图片：`item_potion_red.png`, `item_armor_steel.png`
- 特效图片：`effect_explosion_01.png`, `effect_heal_02.png`

### 2.4 注意事项
- 图片尺寸应符合设计稿要求（750x1334 基准）
- 透明背景图片使用 PNG 格式
- 复杂背景图片使用 JPG 格式以减少体积
- 避免使用中文或特殊字符

## 3. 音频资源命名规范

### 3.1 目录结构
```
StarRush/
└── audio/
    ├── bgm/            # 背景音乐
    ├── sfx/            # 音效
    └── voice/          # 语音
```

### 3.2 命名格式
```
[类型]_[描述]_[变体].mp3/ogg
```

### 3.3 示例
- 背景音乐：`bgm_main_theme.mp3`, `bgm_battle.mp3`
- 音效：`sfx_button_click.mp3`, `sfx_explosion.mp3`
- 语音：`voice_hero_warrior_01.mp3`, `voice_npc_guide_02.mp3`

## 4. 脚本文件命名规范

### 4.1 目录结构
```
StarRush/
└── js/
    ├── core/           # 核心引擎模块
    ├── scenes/         # 场景文件
    ├── ui/             # UI组件
    └── utils/          # 工具函数
```

### 4.2 命名格式
- **核心模块**：`功能-manager.js` 或 `功能-adapter.js`
- **场景文件**：`场景名称-scene.js`
- **UI组件**：`组件名称.js`
- **工具文件**：`功能-manager.js`

### 4.3 示例
- 核心模块：`renderer.js`, `resource-manager.js`, `screen-adapter.js`
- 场景文件：`home-scene.js`, `battle-scene.js`, `shop-scene.js`
- UI组件：`button.js`, `tab-bar.js`, `resource-bar.js`
- 工具文件：`storage-manager.js`

## 5. 数据文件命名规范

### 5.1 目录结构
```
StarRush/
└── data/
    ├── game-data.js    # 游戏核心数据
    ├── hero-data.js    # 英雄数据
    ├── item-data.js    # 物品数据
    └── config-data.js  # 配置数据
```

### 5.2 命名格式
```
[数据类型]-data.js
```

### 5.3 示例
- `game-data.js` - 游戏核心数据
- `hero-data.js` - 英雄属性和技能数据
- `item-data.js` - 物品属性和效果数据
- `config-data.js` - 游戏配置和常量

## 6. 场景文件命名规范

### 6.1 目录
`StarRush/js/scenes/`

### 6.2 命名格式
```
[场景名称]-scene.js
```

### 6.3 示例
- `home-scene.js` - 主界面场景
- `hero-scene.js` - 英雄管理场景
- `battle-scene.js` - 战斗场景
- `shop-scene.js` - 商城场景
- `starmap-scene.js` - 星空地图场景
- `pvp-scene.js` - PVP对战场景
- `login-scene.js` - 登录场景

## 7. UI组件命名规范

### 7.1 目录
`StarRush/js/ui/`

### 7.2 命名格式
- **基础组件**：`组件名称.js`
- **复合组件**：`功能-bar.js` 或 `功能-panel.js`

### 7.3 示例
- 基础组件：`button.js`, `text.js`, `progress-bar.js`
- 复合组件：`tab-bar.js`, `resource-bar.js`, `card.js`

## 8. 常量和变量命名规范

### 8.1 常量命名
- 使用大写字母和下划线
- 例如：`MAX_HEALTH`, `SCREEN_WIDTH`

### 8.2 变量命名
- 使用驼峰命名法
- 例如：`playerHealth`, `enemyCount`

### 8.3 函数命名
- 使用驼峰命名法
- 动词开头，例如：`loadResource()`, `renderScene()`

### 8.4 类命名
- 使用大驼峰命名法
- 例如：`ScreenAdapter`, `ResourceManager`

## 9. 特殊资源命名

### 9.1 动画资源
```
[动画名称]_[帧数]_[尺寸].png
```
示例：`explosion_8_64x64.png`

### 9.2 粒子效果
```
particle_[效果名称]_[变体].png
```
示例：`particle_fire_01.png`

### 9.3 字体文件
```
font_[字体名称]_[大小].ttf
```
示例：`font_main_24.ttf`

## 10. 版本控制

### 10.1 资源版本号
- 对于需要版本管理的资源，在文件名后添加版本号
- 格式：`[资源名称]_v[版本号].ext`
- 示例：`background_main_v2.png`

### 10.2 资源替换
- 当替换资源时，应保留旧版本资源作为备份
- 在版本控制系统中添加适当的注释

## 11. 最佳实践

### 11.1 命名检查
- 创建资源前，检查是否已存在相似命名的资源
- 确保命名符合项目规范

### 11.2 资源组织
- 定期整理资源目录，保持结构清晰
- 移除未使用的资源

### 11.3 文档更新
- 当添加新类型的资源时，更新此命名规范文档
- 确保团队成员了解并遵循命名规范

## 12. 总结

统一的资源命名规范有助于：
- 提高代码可读性和可维护性
- 减少资源管理的混乱
- 方便团队协作和资源共享
- 支持项目的长期发展和扩展

所有团队成员应严格遵守此命名规范，确保项目资源的一致性和专业性。
